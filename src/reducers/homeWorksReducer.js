const initialState = [
  {
    title: 'Deberes de los niños, niñas y adolescentes',
    id: '001',
  },
];
const homeworkReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default homeworkReducer;
